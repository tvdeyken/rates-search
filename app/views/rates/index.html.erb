<p id="notice"><%= notice %></p>

<h1>Rates</h1>

<table>
  <thead>
    <tr>
      <th>Ctry code</th>
      <th>Zip code</th>
      <th>Weight</th>
      <th>Price</th>
      <th>Carrier</th>
      <th>Experation date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rates.each do |rate| %>
      <tr>
        <td><%= rate.ctry_code %></td>
        <td><%= rate.zip_code %></td>
        <td><%= rate.weight %></td>
        <td><%= rate.price %></td>
        <td><%= rate.carrier %></td>
        <td><%= rate.experation_date %></td>
        <td><%= link_to 'Show', rate %></td>
        <td><%= link_to 'Edit', edit_rate_path(rate) %></td>
        <td><%= link_to 'Destroy', rate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rate', new_rate_path %>
